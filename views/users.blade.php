@include('header')
<h1>Hello USer</h1>
@include('inner')

@foreach ($users as $user)
<h3>{{$user}}</h3>
@endforeach
@foreach ($users as $user)
<h3>{{$user}}</h3>
@endforeach
@foreach ($users as $user)
<h3>{{$user}}</h3>
@endforeach

@include('footer')
